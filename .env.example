# Synthetic demo only. Change before sharing even in a private environment.
JWT_SECRET=replace-with-a-long-random-secret
DEMO_PASSWORD=TriNetraDemo!2026
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
DATABASE_MODE=sqlite
DATABASE_URL=sqlite:///./trinetra.db
GRAPH_MODE=local
# Optional full profile only; configure private credentials before deployment.
# DATABASE_MODE=postgresql
# DATABASE_URL=postgresql+psycopg://trinetra@localhost:5432/trinetra
# GRAPH_MODE=neo4j
# NEO4J_URI=bolt://localhost:7687
